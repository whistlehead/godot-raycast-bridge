#pragma once

#include <godot_cpp/core/class_db.hpp>

void initialize_raycast_bridge(godot::ModuleInitializationLevel p_level);
void uninitialize_raycast_bridge(godot::ModuleInitializationLevel p_level);
